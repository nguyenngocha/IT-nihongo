<% if @child_comment %>
  var e = document.getElementById("<%= @comment.id %>");
  $(e).parent().children('li').html("<%= j render 'shared/reply_form', comment: @comment.comment %>");
<% else %>
  $(".form-comment").html("<%= j render 'shared/comment_form' %>");
<% end %>
